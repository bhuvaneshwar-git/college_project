version: '3'
services:
  browser-lab:
    build: .
    ports:
      - "6080:6080"   # noVNC port
      - "5901:5901"   # VNC port (optional)
    volumes:
      - ./data:/data
    restart: unless-stopped
